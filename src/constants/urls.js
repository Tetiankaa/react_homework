const baseURLJson = 'https://jsonplaceholder.typicode.com';

const baseURLCars ='http://owu.linkpc.net/carsAPI/v1';

const posts = '/posts';
const comments = '/comments';
const cars = '/cars';

const urls = {
    cars,
    comments,
    posts
}

export {
    baseURLCars,baseURLJson,urls
}